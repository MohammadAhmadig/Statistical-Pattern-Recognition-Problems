Dataset: Both the training and test datasets consists of several samples of numbers. Each number is represented by a 28x28 pixels(shown by characters) in an ASCII format file. The pixels include three types of characters. ' ' for a white pixel, '+' as a gray pixel and '#' as a black pixel.

Labels: A label file is provided for each set of data which represents each of the sample's label.


